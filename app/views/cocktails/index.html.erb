<div class="container">
  <div class="row">
    <div class="col-xs-12">

      <div class="intro">
        <h1>Voici la liste des cocktails</h1>
        </div>

    <table class="table">
      <tbody>
        <% @cocktails.each do |cocktail| %>
          <tr>
            <td><%= cocktail.name %></td>
            <td><%= link_to 'Voir le détail', cocktail_path(cocktail) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'Ajoutez un cocktail', new_cocktail_path %>

    <br>

   </div>
  </div>
</div>
